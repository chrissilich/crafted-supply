<div
  class="buttons-block {{block.className}}"
  id="{{block.anchor}}"
  {{is_preview ? 'data-preview' : ''}}
>
  <InnerBlocks allowedBlocks="{{innerblocks_allowed}}" template="{{innerblocks_template}}" />
</div>
