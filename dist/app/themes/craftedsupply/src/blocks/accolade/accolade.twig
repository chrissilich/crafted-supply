{% import 'macros.twig' as macros %}

<section{{ block.anchor ? ' id="' ~ block.anchor ~ '"' }}
  {{ is_preview ? 'data-preview' }}
  class="accolade-block"
>
  <p>Accolade Block</p>
  <p>This was generated by <code>artax</code>, like this:</p>
  <code>artax g block accolade</code>

  {# Get content from fields like this #}
  {{ fields.FIELD_NAME }}
</section>
