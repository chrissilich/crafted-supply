{% import 'macros.twig' as macros %}

<section{{ block.anchor ? ' id="' ~ block.anchor ~ '"' }}
  {{ is_preview ? 'data-preview' }}
  class="sophisticated-type-block"
>
  <p class="-{{ fields.layout }}">
    <span>{{ fields.line_1 }}</span>
    <span>{{ fields.line_2 }}</span>
    <span>{{ fields.line_3 }}</span>
  </p>
</section>
