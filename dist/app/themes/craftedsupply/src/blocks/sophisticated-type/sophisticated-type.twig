{% import 'macros.twig' as macros %}

<section{{ block.anchor ? ' id="' ~ block.anchor ~ '"' }}
  {{ is_preview ? 'data-preview' }}
  class="sophisticated-type-block"
>
  <p class="-{{ fields.layout }}">
    <span class="line-wrap line-1-wrap"><span>{{ fields.line_1 }}</span></span>
    <span class="line-wrap line-2-wrap"><span>{{ fields.line_2 }}</span></span>
    <span class="line-wrap line-3-wrap"><span>{{ fields.line_3 }}</span></span>
  </p>
</section>
